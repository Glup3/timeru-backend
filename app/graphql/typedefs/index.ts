import auth from './auth';
import category from './category';
import root from './root';
import shared from './shared';
import user from './user';

const schemaArray = [auth, category, root, shared, user];

export default schemaArray;
